# flashcard-audit — Evolution Log

## 2026-06-03 — Initial creation

- Created skill with 4-criteria quality gate (transferable model, conversation
  fuel, vocabulary, behavior-changing principle)
- Target pool: 500-800 active cards (down from ~7,400)
- Canonical index for cross-file deduplication
- Conversational rewrite style with analogies
- Calibrated on `S05_ISRI_TIS-TAC_P1.md` (Social Identity Theory)
- User preferences: natural language per insight, conversational tone,
  analogies and visualisations, aggressive retirement

## 2026-06-16 — New deck tag `fc/thesis` (introduced by the `reference-flashcards` skill)

- A new card purpose emerged: **thesis-defense cards** built from the curated
  reference library (`Thesis/References/`) by the sibling `reference-flashcards`
  skill, written under `~/ObsVault/1.Projects/RA MSc NEAD/References/`.
- Registered the first 158 such cards (tag `fc/thesis`, 17 reference notes).
- **Deck-specific retention rule** (do NOT apply the standard aggressive
  retirement to this deck): a card is kept if it helps the user *write or
  defend the MSc thesis* — i.e. precise findings/stats, method rationale, or a
  juror Q&A. The standard "too academic / no real-life utility" RETIRE-NARROW
  verdict does NOT apply here; academic precision IS the point. Only retire
  `fc/thesis` cards that are true cross-file duplicates or broken/placeholder.
- Cross-paper reinforcement is intentional, not duplication: the same point
  (e.g. the draining-vein confound) framed from the modelling paper, the
  toolbox, and the empirical control are complementary defense angles — keep.
- Time-boxed: these cards can be bulk-retired after the thesis defense.
